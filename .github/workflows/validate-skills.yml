name: Validate Skills

on:
  push:
    branches:
      - main
    paths:
      - 'skills/**/SKILL.md'
      - 'skills/**/*.py'
      - '.claude/skills/**/SKILL.md'
      - 'scripts/**'
      - 'tests/**'
      - '.claude/rules/**'
      - 'docs/audit/**'
      - '.github/workflows/**'
      - 'Makefile'
      - 'package.json'
      - 'pyproject.toml'
      - '.claude-plugin/plugin.json'
      - '.claude-plugin/marketplace.json'
      - '.release-please-manifest.json'
  pull_request:
    paths:
      - 'skills/**/SKILL.md'
      - 'skills/**/*.py'
      - '.claude/skills/**/SKILL.md'
      - 'scripts/**'
      - 'tests/**'
      - '.claude/rules/**'
      - 'docs/audit/**'
      - '.github/workflows/**'
      - 'Makefile'
      - 'package.json'
      - 'pyproject.toml'
      - '.claude-plugin/plugin.json'
      - '.claude-plugin/marketplace.json'
      - '.release-please-manifest.json'

permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest
    permissions:
      contents: read # read-only: only reads the checkout to validate it
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Install uv
        uses: astral-sh/setup-uv@d31148d669074a8d0a63714ba94f3201e7020bc3 # v8.3.0

      - name: Validate all SKILL.md files
        run: |
          uv run --quiet scripts/validate-skill.py
          uv run --quiet scripts/validate-skill.py .claude/skills/*/SKILL.md

      - name: Validate rules files
        run: uv run --quiet scripts/validate-rules.py

      - name: Check version sync
        run: uv run --quiet scripts/check-version-sync.py

      - name: Check audit findings consistency
        run: uv run --quiet skills/nitpicker/check-audit-consistency.py

      - name: Run tests
        run: uv run --with pytest pytest tests/

      - name: Lint scripts
        run: uv run --with ruff ruff check scripts/ tests/ skills/

      - name: Check script formatting
        run: uv run --with ruff ruff format --check scripts/ tests/ skills/
