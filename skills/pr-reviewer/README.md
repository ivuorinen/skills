# pr-reviewer

Hostile but constructive PR review. Produces copy-paste-ready markdown formatted for GitHub PR comments.

## When to Use

- Reviewing a pull request, staged changes, or a specific diff
- Before merging: "review this PR", "review my diff", "check these changes"
- When you want findings formatted for direct pasting into a GitHub review thread

**When NOT to use:**
- Deep adversarial code-level hunt for logic bugs → use [adversarial-reviewer]
- Full repository audit (not just the diff) → use [nitpicker]
- Implementing review comments that already exist → use [cr-implementer]

## What It Reads / Writes

| | |
|---|---|
| **Reads** | Git diff (default: `git diff main...HEAD`), staged changes, or a specific PR diff |
| **Writes** | stdout only — no findings file is created |

## How to Invoke

```
/pr-reviewer
/pr-reviewer <PR number>
/pr-reviewer <file paths>
```

Default scope is `git diff main...HEAD`. Pass a PR number to fetch via `gh pr diff`, or paths to review specific files.

## Process

1. **Identify the scope** — accept one of: `git diff main...HEAD` (default), `git diff --cached` (staged only), specific file paths, or a PR number fetched via `gh pr diff`
2. **Apply the review checklist** to changed lines only — do not flag issues in unchanged context lines
3. **Record each finding** with severity, file and line, what is wrong, a concrete trigger, and a suggested fix
4. **Emit the review** as a single markdown block — nothing else; no preamble, no summary prose

## Review Checklist

Apply to changed lines only. Skip categories with no relevant constructs in the diff.

| Category | What to look for |
|----------|-----------------|
| **Correctness** | Logic errors, off-by-one, wrong operator, missed condition |
| **Edge cases** | null/empty/zero/max, Unicode, duplicate calls, first/last element |
| **Error handling** | Swallowed errors, missing await, overbroad catch, no cleanup |
| **Security** | Injection, missing auth, secret exposure, trust boundary violations |
| **State & concurrency** | Races, TOCTOU, stale closures, missing synchronisation |
| **Data integrity** | Unvalidated input, partial writes, constraint violations |
| **Resource management** | Leaks, unbounded growth, missing timeouts |
| **Conventions** | Violates patterns established in surrounding unchanged code |

## Output Format

A single markdown block, ready to paste into a GitHub PR comment:

```markdown
## Code Review

### Critical
- **[short title]** — `path/to/file:line`
  [What is wrong — one sentence.]
  **Trigger:** [exact input or sequence]
  **Fix:** [minimal corrective action or code snippet]

### High
...

### Medium
...

### Low
...

---
_Review generated by pr-reviewer. Findings cover changed lines only._
```

If no findings:

```markdown
## Code Review

No issues found in the changed lines.
```

## Common Mistakes to Avoid

- Do NOT report issues in unchanged context lines
- Do NOT omit the `Trigger:` — every finding must be reproducible
- Do NOT write findings to any file
- Do NOT include style opinions, naming preferences, or "I would do it differently"
- Do NOT manufacture issues to seem thorough — silence is correct when nothing is wrong
- Do NOT add a summary paragraph before or after the markdown block

## Related Skills

- [adversarial-reviewer] — deeper hostile code review not limited to the diff
- [cr-implementer] — implements unresolved GitHub PR review comments
- [nitpicker] — full repository audit covering code, tests, docs, and config

---

[skill-source]: SKILL.md
[adversarial-reviewer]: ../adversarial-reviewer/README.md
[cr-implementer]: ../cr-implementer/README.md
[nitpicker]: ../nitpicker/README.md
