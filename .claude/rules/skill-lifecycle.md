# Skill Lifecycle

When creating a new skill with `/new-skill`, complete every phase in sequence.
Never skip any phase: RED (skill-tester baseline) → scaffold → GREEN (skill-tester verify) → REFACTOR → adversarial-reviewer → validate-skills → pr-reviewer → commit.
